import { LimitContribuidoresPipe } from './limit-contribuidores.pipe';

describe('LimitContribuidoresPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitContribuidoresPipe();
    expect(pipe).toBeTruthy();
  });
});
